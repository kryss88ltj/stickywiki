<h1><%= @wiki.title %></h1>
<p><%= @wiki.body %></p>


<%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-info' %>

<%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', confirm: 'Are you sure you want to delete this topic?' %>
